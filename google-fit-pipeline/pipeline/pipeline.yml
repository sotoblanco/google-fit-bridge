name: google-fit-pipeline
schedule: daily
start_date: "2025-01-01"

default_connections:
  bigquery: gcp-default

variables:
  data_types:
    type: array
    items:
      type: string
    default: ["steps", "heart_rate", "sleep"]
